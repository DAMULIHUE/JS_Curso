// CODIGO NÃO EXECUTÁVEL !!!

// Procedural
processamento(valor1, valor2, valor3)

// OO (Orientação à Objetos)
Objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...........
    }
}

objeto.processamento(); // foco passou a ser o obj

// Principios IMPORTANTES:
// 1. Abstração:
//traduzir um obj da vida real pro sistema
carro = {
    // apenas infos pertinentes ao detran:
    placa,
    dono,
    statusFarol,
    statusRodas,
    // apenas infos para mecanica:
    motor,
    Rodas,
    chasi
}

// 2. encapsulamento
// detalhes escondidos ao usuário para evitar
// que ele tenha que se adptar às novas interfaces

// 3. herança
// reuso - composição (carro composto por rodas, motor....)
// reuso - herança (herda caracteristicas do pai)

// 4. polimorfismo
//  a variavel carro pode ter varios valores como: civic, uno, ferrari....



 

